# Yarn 4 Berry - Expo EAS compatible config
# nodeLinker: node-modules is REQUIRED - PnP breaks Expo/EAS Build
nodeLinker: node-modules

# Disable global cache for reproducible EAS Builds
enableGlobalCache: false

# Faster installs (optional)
compressionLevel: mixed
